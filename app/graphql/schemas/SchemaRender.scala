package graphql.schemas

import graphql.GraphQLServer
import sangria.renderer.SchemaRenderer

object SchemaRender {

  def render = {
//    SchemaRenderer.renderSchema(GraphQLServer.graphqlSchema)
  }
}
